<Page x:Class="password_manager.PasswordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:password_manager"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PasswordsPage">

    <Page.Resources>
        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF1a1a2e" Offset="0"/>
            <GradientStop Color="#FF16213e" Offset="1"/>
        </LinearGradientBrush>

        <!-- ListBox Item Style -->
        <Style x:Key="PasswordListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="#FF2a2a4a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        CornerRadius="8">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Button Style -->
        <Style x:Key="IconButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#FFaaaaaa"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FF4a4a8a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF5a5a9a"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource BackgroundGradient}" Margin="0,0,0,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PageName" FontSize="24" FontWeight="Bold" Foreground="White" Margin="20,20,20,30"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SettingsButton" Content="⚙️" Width="30" Height="30" Style="{StaticResource IconButton}" Click="SettingsButton_Click"/>
            <Button x:Name="BackButton" Content="⬅️" Click="BackButton_Click" Width="30" Height="30" Style="{StaticResource IconButton}"/>
        </StackPanel>

        <!-- Passwords List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,0,20,20">
                <ListBox Grid.Row="1" 
                Background="Transparent" 
                BorderThickness="0"
                ItemContainerStyle="{StaticResource PasswordListBoxItem}">
                    <ListBoxItem>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🌐" FontSize="20" Grid.Column="0" Margin="0,0,15,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="example.com" FontWeight="Bold"/>
                                <TextBlock Text="john.doe@example.com" Foreground="#FFaaaaaa"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock x:Name="passwordTextBlock" Text="aa" Foreground="#FFaaaaaa" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" Visibility="Hidden"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3" Orientation="Horizontal">
                                <Button Content="👁" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="ViewPassword_Click"/>
                                <Button Content="🖊️" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="EditButton_Click"/>
                                <Button Content="🗑️" Style="{StaticResource IconButton}" Click="DeletePassword_Click"/>
                            </StackPanel>
                        </Grid>
                    </ListBoxItem>
                    <ListBoxItem>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="📧" FontSize="20" Grid.Column="0" Margin="0,0,15,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Gmail" FontWeight="Bold"/>
                                <TextBlock Text="jane.smith@gmail.com" Foreground="#FFaaaaaa"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock x:Name="passwordTextBlock1" Text="aa" Foreground="#FFaaaaaa" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" Visibility="Hidden"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3" Orientation="Horizontal">
                                <Button Content="👁" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="ViewPassword_Click"/>
                                <Button Content="🖊️" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="EditButton_Click"/>
                                <Button Content="🗑️" Style="{StaticResource IconButton}" Click="DeletePassword_Click"/>
                            </StackPanel>
                        </Grid>
                    </ListBoxItem>
                    <ListBoxItem>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="💼" FontSize="20" Grid.Column="0" Margin="0,0,15,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Work Portal" FontWeight="Bold"/>
                                <TextBlock Text="j.doe@company.com" Foreground="#FFaaaaaa"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock x:Name="passwordTextBlock2" Text="aa" Foreground="#FFaaaaaa" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" Visibility="Hidden"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3" Orientation="Horizontal">
                                <Button Content="👁" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="ViewPassword_Click"/>
                                <Button Content="🖊️" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Click="EditButton_Click"/>
                                <Button Content="🗑️" Style="{StaticResource IconButton}" Click="DeletePassword_Click"/>
                            </StackPanel>
                        </Grid>
                    </ListBoxItem>
                </ListBox>

                <!-- Add New Group Button -->
                <Button Content="➕ Add New Password" Style="{StaticResource ModernButton}" Margin="0,20,0,0" HorizontalAlignment="Left" Click="AddNewPassword_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
